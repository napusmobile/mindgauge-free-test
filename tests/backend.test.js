const test=require('node:test');const assert=require('node:assert/strict');
const backend=require('../backend/Code.js');
const valid={name:'สมชาย ใจดี',consent:true,submissionId:'mg-1234567890abcdef',submittedAt:'2026-08-17T03:00:00.000Z',durationSeconds:420,score:75,level:'ดี',performanceIndex:78,performanceLevel:'มีประสิทธิภาพดี',timeEfficiency:100,averageSecondsPerQuestion:21,categories:{numerical:75,logic:50,verbal:100,spatial:75,attention:75},website:''};
test('accepts a minimal valid result payload',()=>assert.equal(backend.validatePayload_(valid).ok,true));
test('rejects missing consent, invalid score, and unexpected answer data',()=>{assert.equal(backend.validatePayload_({...valid,consent:false}).ok,false);assert.equal(backend.validatePayload_({...valid,score:101}).ok,false);assert.equal(backend.validatePayload_({...valid,answers:[1,2]}).error,'UNEXPECTED_FIELD')});
test('honeypot submissions are recognized',()=>assert.equal(backend.validatePayload_({...valid,website:'bot'}).bot,true));
test('email contains escaped tester name, performance summary, and no answer details',()=>{const e=backend.buildEmail_({...valid,name:'<script>A</script>'});assert.match(e.htmlBody,/&lt;script&gt;A/);assert.match(e.htmlBody,/ดัชนีประสิทธิภาพ/);assert.match(e.htmlBody,/78\/100/);assert.match(e.htmlBody,/เวลาเฉลี่ยต่อข้อ/);assert.doesNotMatch(e.htmlBody,/ลำดับถัดไป|คำตอบของคุณ/);assert.match(e.subject,/MindGauge/)});
test('rejects invalid performance fields',()=>{assert.equal(backend.validatePayload_({...valid,performanceIndex:101}).ok,false);assert.equal(backend.validatePayload_({...valid,timeEfficiency:-1}).ok,false);assert.equal(backend.validatePayload_({...valid,averageSecondsPerQuestion:3601}).ok,false)});
